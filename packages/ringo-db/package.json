{
  "name": "ringo-db",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev:db": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "fast-check": "^4.9.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
