{
  "name": "ringo-bsky",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev:job": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@atproto/api": "^0.12.23"
  }
}
