{
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "typecheck": "bun run --filter='*' typecheck",
    "test": "bun run --filter='*' test",
    "verify": "./verify/verify.sh",
    "cf-typegen": "bun run --filter='*' cf-typegen",
    "postinstall": "bun run --filter='*' cf-typegen"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "wrangler": "^4.110.0"
  }
}
